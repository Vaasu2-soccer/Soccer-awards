const express = require('express');\nconst { authMiddleware, adminOnly } = require('../middleware/auth.middleware');\nconst {\n  getAllAwards,\n  getAwardById,\n  createAward,\n  updateAward,\n  autoAssignWinner,\n  getAwardLeaderboard,\n  deleteAward\n} = require('../controllers/awards.controller');\n\nconst router = express.Router();\n\n// Public routes\nrouter.get('/', getAllAwards);\nrouter.get('/:id', getAwardById);\nrouter.get('/:id/leaderboard', getAwardLeaderboard);\n\n// Admin-only routes\nrouter.post('/', authMiddleware, adminOnly, createAward);\nrouter.put('/:id', authMiddleware, adminOnly, updateAward);\nrouter.post('/:id/auto-assign', authMiddleware, adminOnly, autoAssignWinner);\nrouter.delete('/:id', authMiddleware, adminOnly, deleteAward);\n\nmodule.exports = router;\n